name: quality

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  rust:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-none
          components: rustfmt, clippy
      - name: Run Rust quality gate
        run: make ci
      - name: Install Zenoh router
        run: cargo install zenohd --version 1.10.1 --locked
      - name: Run Zenoh router integration tests
        run: make test-zenoh
      - name: Upload build evidence
        uses: actions/upload-artifact@v4
        with:
          name: esop-build-evidence
          path: |
            build/*_report.json
            qualification/*.json
            qualification/KNOWN_LIMITATIONS.md
          if-no-files-found: error

  bpf:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install eBPF build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang
      - name: Build CO-RE BPF object
        run: |
          mkdir -p bpf/build
          cp bpf/vmlinux.h bpf/build/vmlinux.h
          make -C bpf

  ebpf-gateway-runtime:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Install eBPF build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang
      - name: Run privileged gateway runtime qualification
        run: make test-ebpf-gateway-runtime
      - name: Upload gateway runtime qualification
        uses: actions/upload-artifact@v4
        with:
          name: esop-ebpf-gateway-runtime-qualification
          path: build/ebpf_gateway_qualification.json
          if-no-files-found: error

  ebpf-raw-port-runtime:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Install eBPF build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang
      - name: Run privileged raw-port runtime qualification
        run: make test-ebpf-raw-port-runtime
      - name: Upload raw-port runtime qualification
        uses: actions/upload-artifact@v4
        with:
          name: esop-ebpf-raw-port-runtime-qualification
          path: build/ebpf_raw_port_qualification.json
          if-no-files-found: error

  ebpf-process-exit-runtime:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Install eBPF build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang
      - name: Run privileged process-exit runtime qualification
        run: make test-ebpf-process-exit-runtime
      - name: Upload process-exit runtime qualification
        uses: actions/upload-artifact@v4
        with:
          name: esop-ebpf-process-exit-runtime-qualification
          path: build/ebpf_process_exit_qualification.json
          if-no-files-found: error

  ebpf-scheduler-migration-runtime:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Install eBPF build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang
      - name: Run privileged scheduler migration runtime qualification
        run: make test-ebpf-scheduler-migration-runtime
      - name: Upload scheduler migration runtime qualification
        uses: actions/upload-artifact@v4
        with:
          name: esop-ebpf-scheduler-migration-runtime-qualification
          path: build/ebpf_scheduler_migration_qualification.json
          if-no-files-found: error

  ebpf-scheduler-runqueue-runtime:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Install eBPF build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang
      - name: Run privileged scheduler runqueue runtime qualification
        run: make test-ebpf-scheduler-runqueue-runtime
      - name: Upload scheduler runqueue runtime qualification
        uses: actions/upload-artifact@v4
        with:
          name: esop-ebpf-scheduler-runqueue-runtime-qualification
          path: build/ebpf_scheduler_runqueue_qualification.json
          if-no-files-found: error
